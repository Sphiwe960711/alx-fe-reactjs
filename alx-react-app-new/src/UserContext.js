// src/UserContext.js
import { createContext } from 'react';

// Create a context with default value of null (or you can set it to an empty object if you prefer)
export const UserContext = createContext(null);
